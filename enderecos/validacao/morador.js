// let servicoBusca = require("../../vacinados/busca/main.js");

exports.idEValido = async function(id) {
  // let vacinadoExiste            = await servicoBusca.busca(id) != null;
  // let vacinadoNaoPossuiEndereco = await servicoBusca.possuiEndereco(id) == false;
  // return vacinadoExiste && vacinadoNaoPossuiEndereco;
  return true;
}